
name: 'google_ads_demo'
version: '0.1'
profile: 'google_ads_demo'
source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"
    - "logs"

models:
  google_ads_demo: 
      materialized: table
      staging:
        materialized: view
        tags: ["staging", "hourly"]
